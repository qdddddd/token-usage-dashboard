{
  "name": "token-usage-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Web dashboard for token usage across multiple AI providers",
  "scripts": {
    "start": "node server.js",
    "install-browsers": "npx playwright install chromium",
    "setup-profile": "node setup-profile.js"
  },
  "dependencies": {
    "playwright": "^1.40.0",
    "dotenv": "^16.0.0"
  }
}
